# Finding the subsequence with the largest sum
Brute Force, Prefix Sum, Divide & Conquer and Kadane algorithm 
implementation to find max subArray by Python and draw graph to algorithm complexity comparison 
